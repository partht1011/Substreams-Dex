version: v1
plugins:
  # Use a stable plugin to avoid the prost panic. Output to a temp folder not used by Cargo.
  - plugin: buf.build/protocolbuffers/go
    out: target/buf-go
    opt:
      - paths=source_relative
